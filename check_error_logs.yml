---
- name: Check Error Logs on CentOS/Ubuntu
  hosts: all
  gather_facts: no

  vars:
    # Số dòng error cuối cùng muốn hiển thị (để trống hoặc 0 sẽ lấy 1000 dòng gần nhất)
    error_lines_count: 50
    # Các từ khóa để tìm error (có thể tùy chỉnh)
    error_keywords:
      - error
      - ERROR
      - failed
      - FAILED
      - failure
      - FAILURE
      - critical
      - CRITICAL
      - fatal
      - FATAL
      - exception
      - EXCEPTION
      - exited
      - status=[0-9]+/EXEC

    # Các nguồn/service cần loại trừ khỏi kết quả lỗi (không kiểm tra)
    exclude_error_sources:
      - nrpe
      - nrpe.service
      - setroubleshoot
      - setroubleshootd

  tasks:
    - name: Detect OS by checking log files (fallback method)
      shell: |
        if [ -f /var/log/messages ]; then
          echo "redhat"
        elif [ -f /var/log/syslog ]; then
          echo "debian"
        else
          echo "unknown"
        fi
      register: os_detect_by_log
      changed_when: false
      failed_when: false

    - name: Try to detect OS by reading /etc/os-release (without sudo)
      shell: |
        if [ -f /etc/os-release ]; then
          . /etc/os-release
          echo "$ID"
          echo "$VERSION_ID"
          echo "$NAME"
        elif [ -f /etc/redhat-release ]; then
          cat /etc/redhat-release
        elif [ -f /etc/lsb-release ]; then
          . /etc/lsb-release
          echo "$DISTRIB_ID"
          echo "$DISTRIB_RELEASE"
        else
          echo ""
        fi
      register: os_detect
      changed_when: false
      failed_when: false

    - name: Try to detect OS by reading /etc/os-release (with sudo)
      shell: |
        if [ -f /etc/os-release ]; then
          . /etc/os-release
          echo "$ID"
          echo "$VERSION_ID"
          echo "$NAME"
        elif [ -f /etc/redhat-release ]; then
          cat /etc/redhat-release
        elif [ -f /etc/lsb-release ]; then
          . /etc/lsb-release
          echo "$DISTRIB_ID"
          echo "$DISTRIB_RELEASE"
        else
          echo ""
        fi
      become: yes
      become_method: sudo
      register: os_detect_sudo
      when: os_detect.stdout == "" or os_detect.stdout_lines | length == 0
      changed_when: false
      failed_when: false

    - name: Initialize os_detect_sudo if not defined
      set_fact:
        os_detect_sudo: {"stdout": "", "stdout_lines": []}
      when: os_detect_sudo is not defined

    - name: Set final OS detection result
      set_fact:
        os_detect_final: "{{ os_detect_sudo if (os_detect_sudo.stdout is defined and os_detect_sudo.stdout != '') else (os_detect if os_detect.stdout is defined else {'stdout': '', 'stdout_lines': []}) }}"

    - name: Initialize OS variables from log files
      set_fact:
        os_id: "{{ os_detect_by_log.stdout | default('unknown') | lower }}"
        os_version: "unknown"
        os_name: "{{ 'CentOS/RHEL' if os_detect_by_log.stdout == 'redhat' else ('Ubuntu/Debian' if os_detect_by_log.stdout == 'debian' else 'Unknown') }}"

    - name: Parse OS information from /etc/os-release (if available)
      set_fact:
        os_id: "{{ os_detect_final.stdout_lines[0] | default(os_id) | lower }}"
        os_version: "{{ os_detect_final.stdout_lines[1] | default(os_version) }}"
        os_name: "{{ os_detect_final.stdout_lines[2] | default(os_detect_final.stdout_lines[0] | default(os_name)) }}"
      when: 
        - os_detect_final.stdout is defined
        - os_detect_final.stdout != ""
        - os_detect_final.stdout_lines | length > 0

    - name: Determine OS family
      set_fact:
        is_redhat_family: "{{ os_id in ['rhel', 'centos', 'rocky', 'almalinux', 'fedora', 'oracle', 'redhat'] or 'redhat' in (os_id | default('')) or os_detect_by_log.stdout | default('') == 'redhat' }}"
        is_debian_family: "{{ os_id in ['ubuntu', 'debian'] or os_detect_by_log.stdout | default('') == 'debian' }}"

    - name: Set log file path based on OS
      set_fact:
        log_file: "{{ '/var/log/messages' if is_redhat_family else '/var/log/syslog' }}"
        os_family: "{{ 'RedHat' if is_redhat_family else ('Debian' if is_debian_family else 'Unknown') }}"

    - name: Display OS information
      debug:
        msg: 
          - "Detected OS: {{ os_name }} ({{ os_id }}) {{ os_version }}"
          - "OS Family: {{ os_family }}"
          - "Using log file: {{ log_file }}"

    - name: Read error logs from last N lines (Debian family)
      shell: |
        set -o pipefail
        lines={{ error_lines_count | default(1000) | int }}
        pattern="{{ error_keywords | join('|') }}"
        exclude_pattern="{{ (exclude_error_sources | default([])) | join('|') }}"
        if [ $lines -gt 0 ]; then
          if [ -n "$exclude_pattern" ]; then
            grep -iE "$pattern" {{ log_file }} | grep -viE "$exclude_pattern" | tr -d "\r" | tail -n $lines
          else
            grep -iE "$pattern" {{ log_file }} | tr -d "\r" | tail -n $lines
          fi
        else
          if [ -n "$exclude_pattern" ]; then
            grep -iE "$pattern" {{ log_file }} | grep -viE "$exclude_pattern" | tr -d "\r" | tail -n 1000
          else
            grep -iE "$pattern" {{ log_file }} | tr -d "\r" | tail -n 1000
          fi
        fi
      args:
        executable: /bin/bash
      register: error_logs_debian
      when: is_debian_family
      changed_when: false
      failed_when: false
      ignore_errors: no

    

    - name: Read error logs from last N lines (Redhat family)
      shell: |
        set -o pipefail
        lines={{ error_lines_count | default(1000) | int }}
        pattern="{{ error_keywords | join('|') }}"
        exclude_pattern="{{ (exclude_error_sources | default([])) | join('|') }}"
        if [ $lines -gt 0 ]; then
          if [ -n "$exclude_pattern" ]; then
            grep -iE "$pattern" {{ log_file }} | grep -viE "$exclude_pattern" | tr -d '\r' | tail -n $lines
          else
            grep -iE "$pattern" {{ log_file }} | tr -d '\r' | tail -n $lines
          fi
        else
          if [ -n "$exclude_pattern" ]; then
            grep -iE "$pattern" {{ log_file }} | grep -viE "$exclude_pattern" | tr -d '\r' | tail -n 1000
          else
            grep -iE "$pattern" {{ log_file }} | tr -d '\r' | tail -n 1000
          fi
        fi
      args:
        executable: /bin/bash
      register: error_logs_redhat 
      when: is_redhat_family
      changed_when: false
      failed_when: false
      ignore_errors: no

    - name: Read error logs via journalctl (Debian family fallback)
      shell: |
        set -o pipefail
        lines={{ error_lines_count | default(1000) | int }}
        pattern="{{ error_keywords | join('|') }}"
        exclude_pattern="{{ (exclude_error_sources | default([])) | join('|') }}"
        if command -v journalctl >/dev/null 2>&1; then
          if [ $lines -gt 0 ]; then
            if [ -n "$exclude_pattern" ]; then
              journalctl --no-pager -o short-iso -a | grep -iE "$pattern" | grep -viE "$exclude_pattern" | tail -n $lines
            else
              journalctl --no-pager -o short-iso -a | grep -iE "$pattern" | tail -n $lines
            fi
          else
            if [ -n "$exclude_pattern" ]; then
              journalctl --no-pager -o short-iso -a | grep -iE "$pattern" | grep -viE "$exclude_pattern" | tail -n 1000
            else
              journalctl --no-pager -o short-iso -a | grep -iE "$pattern" | tail -n 1000
            fi
          fi
        else
          # journalctl not available
          exit 0
        fi
      args:
        executable: /bin/bash
      register: error_logs_journal
      when:
        - is_debian_family
        - error_logs_debian is defined
        - (error_logs_debian.stdout_lines | default([]) | length) == 0
      changed_when: false
      failed_when: false
      ignore_errors: yes

    - name: Merge results into unified variable
      set_fact:
        error_logs: "{{
          error_logs_redhat if (error_logs_redhat is defined and (error_logs_redhat.stdout_lines | default([]) | length) > 0)
          else (
            error_logs_debian if (error_logs_debian is defined and (error_logs_debian.stdout_lines | default([]) | length) > 0)
            else (
              error_logs_journal if (error_logs_journal is defined)
              else {'stdout': '', 'stdout_lines': []}
            )
          )
        }}"

    - name: Debug - Show grep pattern and result
      debug:
        msg: 
          - "Grep pattern: {{ error_keywords | join('|') }}"
          - "Reading from last {{ error_lines_count if error_lines_count > 0 else 1000 }} lines"
          - "Error logs found: {{ error_logs.stdout_lines | default([]) | length }} lines"
          - "Error logs rc: {{ error_logs.rc | default('N/A') }}"
      when: error_logs is defined

    - name: Initialize error_logs if not defined
      set_fact:
        error_logs: {"stdout": "", "stdout_lines": [], "rc": 1}
      when: error_logs is not defined

    - name: Ensure error_logs has stdout_lines
      set_fact:
        error_logs: "{{ error_logs | combine({'stdout_lines': error_logs.stdout_lines | default([])}) }}"
      when: error_logs is defined
      failed_when: false
      changed_when: false

    - name: Count total error lines
      set_fact:
        total_error_lines: "{{ (error_logs.stdout_lines | default([])) | length }}"
      when: error_logs is defined

    - name: Initialize total_error_lines if not set
      set_fact:
        total_error_lines: 0
      when: error_logs is not defined

    - name: Display error logs
      debug:
        msg: "{{ error_logs.stdout_lines }}"
      when: 
        - error_logs is defined
        - error_logs.stdout_lines is defined
        - error_logs.stdout_lines | length > 0

    - name: Show message if no errors found
      debug:
        msg: "✅ No error logs found in {{ log_file }}"
      when: 
        - error_logs is defined
        - (error_logs.stdout_lines is not defined) or (error_logs.stdout_lines | length == 0)

    - name: Display summary
      debug:
        msg: 
          - "📊 Summary:"
          - "  Log file: {{ log_file }}"
          - "  Total error lines found: {{ total_error_lines | default(0) }}"
          - "  Displayed: {{ error_lines_count if error_lines_count > 0 else 1000 }} lines"

